# Server
PORT=5002
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3002

# Database (same PostgreSQL DB as patient backend)
DATABASE_URL="postgresql://user:password@host:5432/railway"

# JWT
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRES_IN=7d

# Tigris (or any S3-compatible) storage — same bucket as doctor backend
S3_ENDPOINT=https://t3.storageapi.dev
S3_REGION=auto
S3_BUCKET_NAME=your_bucket_name
S3_ACCESS_KEY_ID=your_access_key_id
S3_SECRET_ACCESS_KEY=your_secret_access_key

# VAPI / Retell webhook verification secret
VAPI_WEBHOOK_SECRET=your_vapi_webhook_secret_here

# Google Calendar availability sync — must match the doctor backend's OAuth app.
# Admin backend only reads free/busy and writes events; no redirect URI needed.
GOOGLE_CLIENT_ID=your_google_oauth_client_id
GOOGLE_CLIENT_SECRET=your_google_oauth_client_secret
GOOGLE_TOKEN_ENC_KEY=your_long_random_token_encryption_key
